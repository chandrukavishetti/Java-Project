package com.chandru.string_assignment.model;

public class ProductCodeParserModel {
	
	private String input;
	
	public Product(String input) {
		this.input=input;
	}
	public void ProductCodeParser() {
		try {
			input=input.trim();
			String[] data=input.split("-");
			char delimiter='-';
			
			if(!input.contains("-")) {
				throw new Exception("the input should contains the '-' the symbol");
			}
			
			int index=input.indexOf("-");
			
			if(index==0 || index==input.length()-1) {
				throw new Exception("name cannot start and ends with this symbol '-' ");
			}
			
			String category=input.substring(0,index);
			
			String prodname=input.substring(index, delimiter);
			
			String manufacture_year=input.substring(delimiter+1,input.length());
			
			String upper_case=input.toUpperCase();
			
			boolean startswith=input.startsWith("TV");
			
			boolean endswith=input.endsWith("2023");
			
			String replace_to_space=input.replace("-", " ");
			
			int position_of_1st_hyphen=index;
			
			System.out.println("category    : "+category);
			System.out.println("product     : "+prodname);
			System.out.println("manufacture year : "+manufacture_year);
			System.out.println("Start with the TV : "+startswith);
			System.out.println("ends with 2023"+endswith);
		}catch(Exception e) {
			System.out.println("error : "+e.getMessage());
		}
	}

}
